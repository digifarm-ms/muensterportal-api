# API keys — required, no defaults.
MISTRAL_API_KEY=
TAVILY_API_KEY=

# LLM backend. Default in config.py is "ollama" (expects localhost:11434).
# Production uses "mistral" via the OpenAI-compatible endpoint.
LLM_PROVIDER=mistral
