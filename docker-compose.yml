version: "3.9"
services:
  backend:
    restart: unless-stopped
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - /home/deploy/lancedb:/app/lancedb
    container_name: muensterportal-api
